import { SurveyContainer } from '@/components/survey/SurveyContainer';

export default function SurveyPage() {
    return (
        <main className="min-h-screen bg-[#0f172a] text-slate-100 font-sans selection:bg-cyan-500/30">
            <SurveyContainer />
        </main>
    );
}
